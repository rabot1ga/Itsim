# 💻 IT Life Simulator v2.0

**Telegram Mini App** — юмористический симулятор жизни IT-специалиста.

> «Блин, это буквально мой прошлый вторник.»

Игрок начинает после института (10 000 ₽, без работы) и строит карьеру: учит навыки,
работает, ходит на собеседования, получает повышения, обрастает вещами и питомцами,
следит за деньгами, здоровьем, мотивацией, энергией и репутацией. Путь — от безработного
до CTO через выборы борта, с альтернативными финалами (выгорание, уход из IT).

---

## 📑 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Архитектура](#️-архитектура)
- [Игра: ресурсы, цикл, грейды, финалы](#-игра-ресурсы-цикл-грейды-финалы)
- [Механики](#-механики)
- [Визуал: слои, генетика, пиксель-арт](#-визуал-слои-генетика-пиксель-арт)
- [Solana и NFT (mock)](#-solana-и-nft-mock)
- [Клиент](#-клиент)
- [Сервер и API](#-сервер-и-api)
- [Telegram-бот](#-telegram-бот)
- [Контент](#-контент)
- [Баланс и симулятор](#-баланс-и-симулятор)
- [Инструменты](#️-инструменты)
- [Проверки](#-проверки)
- [Карта документации](#-карта-документации)
- [To-Do: что сделано, что в процессе, что предстоит](#-to-do-что-сделано-что-в-процессе-что-предстоит)
- [Лицензия](#-лицензия)

---

## 🚀 Быстрый старт

### Требования

- Node.js 18+ (рекомендуется 20 LTS)
- npm 9+
- Python 3 (только для генераторов SVG-слоёв в `tools/`)

### Запуск

```bash
npm install
npm run dev
```

Поднимутся три процесса:

| Сервис | Адрес | Что это |
|---|---|---|
| Game API | `http://localhost:3001` | Fastify-сервер, сервер-авторитет |
| Mini App клиент | `http://localhost:5173` | React + Vite (прокси `/api` → 3001) |
| Telegram-бот | `http://localhost:3002` | мок-режим без `BOT_TOKEN` |

Отдельно:

```bash
npm run dev -w packages/server   # только API
npm run dev -w packages/client   # только клиент
npm run dev -w packages/bot      # только бот
```

### Переменные окружения

Полный список с комментариями — в [`.env.example`](.env.example) (`cp .env.example .env`).
Ключевые:

| Переменная | Назначение | Без неё |
|---|---|---|
| `BOT_TOKEN` | валидация Telegram initData, Bot API (бот, счета Stars) | dev-режим: initData не проверяется, бот работает в mock-режиме |
| `JWT_SECRET` | подпись токенов `/api/auth/telegram` | дефолт `'dev-secret'` (только для локалки!) |
| `TELEGRAM_WEBHOOK_SECRET` | аутентификация вебхука платежей | `/api/payments/webhook` отвечает 503 |
| `ADMIN_TOKEN` | общий секрет сервера и бота (`/api/admin/*`) | у бота не работают `/stats` и `/delete_my_data` |
| `MINI_APP_URL` | URL мини-приложения для кнопки «Играть» | `https://itsim.app` |

> 🔒 В `NODE_ENV=production` сервер **не стартует** с dev-дефолтами: пустой `BOT_TOKEN`,
> `JWT_SECRET=dev-secret`, пустой `TELEGRAM_WEBHOOK_SECRET` или включённые mock-платежи —
> это ошибка запуска, а не предупреждение (`packages/server/src/config.ts`).
> Деплой описан в [docs/deploy.md](docs/deploy.md).

---

## 🏗️ Архитектура

npm-workspaces монорепозиторий, 6 пакетов:

```
it-life-simulator/
├── packages/
│   ├── shared/     # игровой движок: чистые функции, Zod-схемы, типы, миграции сейвов
│   ├── content/    # весь контент в JSON + загрузчик/валидатор (CLI для CI)
│   ├── server/     # Fastify API, сервер-авторитет, персистентность, платежи
│   ├── client/     # React 18 + Vite 5 + Tailwind + Zustand 4
│   ├── bot/        # Telegram-бот: long polling / webhook, команды, уведомления
│   └── sim/        # симулятор баланса + статистика событий
├── tools/
│   ├── pixelgen/                 # пайплайн пиксельных персонажей (CLI)
│   ├── generate_layer_assets.py  # генератор SVG-слоёв (комната/аватар)
│   └── generate_office_assets.py # генератор SVG-слоёв офиса
├── docs/
│   ├── design.md    # дизайн-документ клиента (§1–16 + роадмап §15, 10/10 закрыт)
│   ├── deploy.md    # деплой, переменные окружения, вебхуки, бэкапы, GDPR
│   └── pixel-art.md # ТЗ пиксельного пайплайна
├── ci/github-actions-ci.yml  # workflow: типы + eslint + контент + тесты + симулятор + сборка
├── .env.example              # все переменные окружения с комментариями
├── SPEC.md    # техническое задание v2.0
├── DESIGN.md  # процедурная генерация + Solana-интеграция
└── ANALYSIS-2026-09-07.md  # аудит проекта на живой сборке
```

| Пакет | Роль | Объём |
|---|---|---|
| `shared` | движок (чистые функции) + схемы + типы + миграции сейвов | ~1.5k строк, 128 юнит-тестов |
| `content` | контент в JSON + загрузчик/валидатор | 37 файлов, ~600 KB, CLI-гейт |
| `server` | Fastify 4, сервер-авторитет | ~3.3k строк (`routes/game.ts` — ~1900), 51 тест |
| `client` | React 18 + Vite 5 + Tailwind + Zustand | 8 экранов + меню |
| `bot` | Telegram-бот (Bot API, polling/webhook/mock) | 4 файла, 22 теста |
| `sim` | симулятор баланса + тулстатистика событий | 2 скрипта |

### Ключевые архитектурные решения

1. **Сервер-авторитет.** Вся игровая логика — на сервере. Клиент отправляет
   намерение (intent), сервер вычисляет эффекты. Клиент использует те же функции
   из `@itsim/shared` для оптимистичного предпросмотра, но источник правды — сервер.
2. **Offline-начисление энергии.** 1 игровой день = 3.5 реальных часа, максимум 7 дней
   в банке. Расчёт только по серверному времени (`lastTickAt`, `bankedDays`).
3. **Контент как данные.** Весь гейм-контент в JSON. Правка баланса = правка JSON,
   без релиза клиента. Валидация через Zod живёт в самом пакете `@itsim/content`:
   один и тот же код гоняют сервер при старте и CI (`npm run validate:content`),
   cross-reference проверки возвращают структурированные issues (error/warning).
4. **Движок — чистые функции.** `packages/shared/src/engine/` не знает ни про HTTP,
   ни про React: одни и те же функции используют сервер, клиент и симулятор.
5. **Узкие интерфейсы под замену.** Хранилище (`load/save/delete`), NFT-провайдер
   (`NftProvider`) — интерфейсы; сейчас за ними JSON-файлы и мок, в проде — PostgreSQL
   и Helius RPC без переделки роутов.
6. **Сейвы версионируются.** У каждого сохранения есть `version`; при загрузке оно
   прогоняется через `migrateState()` (`shared/engine/migrations.ts`) и перезаписывается,
   если что-то изменилось. Миграции идемпотентны, а сейв, записанный более новой версией
   сервера, не трогается — откат билда не портит данные.
7. **Безопасные дефолты.** Все переменные окружения — в `packages/server/src/config.ts`;
   dev-дефолты (незаданный `BOT_TOKEN`, `JWT_SECRET=dev-secret`, mock-платежи) в
   `NODE_ENV=production` приводят к отказу запуска, а не к тихой дыре.

---

## 🎮 Игра: ресурсы, цикл, грейды, финалы

### Ресурсы

| Ресурс | Диапазон | Роль |
|---|---|---|
| 💰 Деньги | 0+ | ограничитель early game; еда, аренда, курсы, переезды |
| ❤️ Здоровье | 0–100 | долгосрочный ресурс; падает от переработок, лечится отдыхом |
| 🔥 Мотивация | 0–100 | краткосрочный ограничитель; дрейфует вниз каждый день |
| ⚡ Энергия | 3–16 | ресурс хода; макс. зависит от жилья и перков |
| ⭐ Репутация | 0–100 | гейт карьеры; растёт от работы и нетворкинга |

### Игровой цикл

День → действия за энергию (учёба / работа / отдых / соц.) → событие с выбором →
«Завершить день» (нативная MainButton в Telegram) → зарплата/расходы/офферы →
новый день. Офлайн игра «ждёт»: энергия копится в банке дней.

### Этапы и грейды

| Этап | Дни | Что происходит |
|---|---|---|
| Early «Выживание» | 1–30 | навыки с нуля, первый оффер |
| Mid «Рост» | 30–150 | junior → middle → senior |
| Late «Влияние» | 150+ | teamlead → architect → выборы CTO |

Лестница: `unemployed → intern → junior → middle → senior → teamlead → architect → cto`.

### Финалы (`balance.endings`)

- 🏢 **Корпоративный бог** — CTO через выборы борта (special-gate, не повышение)
- 🚀 **Экзит** — стартап за 100 млн+
- 💻 **Свободный художник** — фриланс
- 🎓 **Учитель** — 50 джунов
- 🔥 **Выгорание** — терминальное состояние (мотивация/здоровье в нуле)
- 💀 **Ушёл из IT** — терминальное состояние

---

## 🔧 Механики

### ⚡ Энергия и офлайн-прогресс

- Текущая энергия (`energy/maxEnergy`, база 10) тратится на действия, восстанавливается сном и новым днём.
- Максимум энергии растёт от жилья и перков.
- Офлайн: 1 banked-день за 3.5 часа отсутствия, кап — 7 дней; трата — «полная энергия + 10 🔥».
- Всё считается по серверному времени, в `/state` отдаётся `offlineDaysEarned`.

### 💼 Карьера: гейты, ревью, конкуренция

Гейты грейдов — данные (`balance.careerGates`), а не константы в коде. Каждый грейд требует:

- глубину **основного** навыка (`mainSkillDepth`) и сумму по **его ветке** (`branchTotal`),
- общую ширину (`totalLevels`), коммуникацию / английский / лидерство,
- репутацию, минимум дней в грейде (`minDaysInGrade`).

Повышение — двухступенчатое: требования открывают **ревью**, а решает **слот компании**:
шанс `1 / (1 + competition · e^(−0.7·surplus))`. Квалификация «впритык» — долгое ожидание,
перекач — быстрый проход. Клиент показывает это карточкой `CareerPressureCard`
(стоимость дня + чего не хватает + кнопка выборов CTO).

### 🗳️ Выборы CTO

CTO — не повышение, а `special`-гейт: экшен `cto_elect` с кулдауном `electionIntervalDays`.
Шанс — от репутации и лидерства; провал — минус репутация/мотивация. Проверено смоук-тестом
(архитектор rep 80/lead 34 → шанс 62%).

### 🎤 Собеседования-квиз

- 56 вопросов по 14 веткам навыков и 3 уровням грейда.
- Раздача «topic-first»: сначала вопросы основного навыка игрока, потом остальные подходящего уровня.
- Ответы с сервера не утекают; каждый ответ — обучение: верный +3 XP, неверный +2 XP + объяснение.
- `answerScore` 0.4–1.0 честно подмешивается в `interviewChance` (шанс оффера).
- Пропустил день собеседования — авторассмотрение; начал — квиз из 3 вопросов с резюме.

### 🎲 События и цепочки

- **205 уникальных событий в 19 файлах**: daily, быт, погода, работа, учёба, деньги,
  семья, здоровье, соц., tech, mining, side-job будни, NPC-сюжеты, startup- и кот-цепочки,
  по 2 события на каждую ветку навыков, 44 follow-up'а на действия игрока.
- Условия: `cooldown`, `minGameDay`, `minMoney`, `maxOccurrences`, теги + `recentEventTags`
  (защита от повторов), отношения с NPC.
- Цепочки: `chainOnly`-события не попадают в случайный пул, приходят с задержкой `delayDays`.
- Статистика покрытия: `npx tsx packages/sim/src/events_stats.ts` — сколько событий видит
  игрок за 365 дней и размер пространства исходов.

### 🌳 Навыки, перки, мягкие навыки

- **34 навыка в 9 ветках**: frontend, backend, mobile, qa, devops, ai_ml, cybersec,
  gamedev, blockchain (Solidity, Web3, Rust для Solana, DeFi…). Гейтинг родитель → ребёнок,
  XP-кривая, шеринг XP по ветке (`branchShareXp`).
- **Выбор основного навыка**: тап по навыку в дереве — учёба/работа качают именно его
  (заблокированные ветки гейтятся на сервере).
- **8 перков** с реальными эффектами: энергия, скорость обучения, +% к фрилансу/майнингу/подработкам
  («Золотая лихорадка», «Подработчик»…). Открываются при выполнении требований.
- **Мягкие навыки** (коммуникация, английский, тайм-менеджмент, лидерство): нетворкинг —
  1 раз в день (`networking.dailyCap`), выше порога насыщения XP идёт с демпфером —
  митапы нельзя фармить бесконечно.
- **Английский**: бесплатные уроки и платный курс; открывает зарубежные компании.

### 💸 Стоимость жизни

- Бытовые расходы (`livingCosts`) списываются каждый день и растут с грейдом и жильём;
  при нуле денег — режим «гречка» (расходы урезаются, а не смерть).
- **Налог на капитал** (`wealthTax*`) раз в месяц: деньги «под матрасом» тают —
  день должен во что-то превращаться.
- **Переезды — привычка**: нужна подушка `saveMult ×` месячного платежа, удержанная
  `saveStreakDays` подряд (от 3×/25 дн до 15×/70 дн) + доходный барьер (`incomeGateMult`).
- Жильё 0–4 (Общага → Пентхаус): +maxEnergy, +мотивация/репутация; **переезд меняет фон комнаты**.

### 🖥️ Фриланс, 🛵 подработки, ⛏️ майнинг

- **Фриланс**: заказы трёх сложностей с кулдаунами; доход зависит от навыков;
  cross-бонус SMB Gen2 +5%.
- **Подработки (не IT)**: курьер, бариста, грузчик, таксист, ночной охранник,
  репетитор (платит от уровня навыка), стример — 1 в день, цена — энергия/здоровье/мотивация.
- **Майнинг-ферма**: пассивный крипто-доход. Оборудование (видеокарта → ферма → ASIC),
  счёт за электричество, солнечные панели (−50%), детерминированный волатильный курс.
  Ачивки «Подпольный майнер» (100k с фермы) и «Криптокит» (1M).

### 🐾 Питомцы 2.0

- 6 видов: кот, пёс, кактус, робот-пылесос, паук, бульдог. Кормление — 500 ₽/день
  (+мотивация, daily-кап `petFedToday`).
- Настроение от факта кормления (😿 голоден / 😋 сыт), CSS-анимация покачивания,
  статус-чип в комнате.
- Косметика из магазина: бант (3k), очки (5k), корона (15k) — рисуются поверх питомца,
  за питомца не считаются (проверено: кормление с одним бантом отклоняется).

### 🏆 Ачивки, челленджи, лидерборд, NPC

- **17 ачивок** с прогресс-барами на экране «Трофеи» (карьера, деньги, майнинг, события…).
- **13 daily-челленджей**: одно задание в день с прогресс-баром.
- **Лидерборд**: живой рейтинг по карьере/навыкам/деньгам/репутации/ачивкам/жилью,
  считается из персистентных состояний игроков.
- **5 NPC** с отношениями (`relation.X` в эффектах событий) и собственными сюжетными событиями.
- **10 компаний** с культурами-модификаторами, вилками зарплат и требованиями к английскому.

---

## 🎨 Визуал: слои, генетика, пиксель-арт

Стратегия (см. [DESIGN.md](DESIGN.md)): **комбинаторика слоёв + тинтинг + on-chain seed**.
Никаких «готовых картинок комнат» — интерьер и персонаж собираются движком.

### Слойные манифесты (HashLips-паттерн)

| Манифест | Слоты | Записей |
|---|---|---|
| `avatar_manifest` | body, eyes, hair, beard, top, accessory | 36 |
| `room_manifest` | bg, window, decor, desk, chair, setup, atmosphere, pet | 50 |
| `office_manifest` | фон, город, доска, команда, стол, микродетали, 3 коллеги, кофе, настроение | 53 |

- 84+ SVG-слоя в `packages/client/public/layers/` (генерируются скриптами `tools/`,
  художник правит файлы — код не трогаем).
- Grayscale + CSS-тинтинг: 1 ассет × 12 цветов = 12 предметов.
- Редкости `common/rare/legendary`, веса, `excludeWith`, z-order.

### Генетика

Детерминированный генотип: `sha256(wallet|telegramId)` → xmur3+mulberry32 →
weighted pick по слотам. Базовые черты (глаза, причёска, окно…) привязаны к игроку,
сид нельзя «рероллить». Конфиг — `content/genetics.json`.

### Редактор комнаты и гардероб

- Экран «Дом»: живое превью + «🎨 Настроить» → слоты → карусель вариантов.
- Правила разблокировки — в shared-движке (`roomDecor.ts`): часть декора за вещи
  (кресло/ПК открывают слоты), постер Mad Lads — за коллекцию, пиратский — за SMB Gen2.
- **Гардероб** (`Wardrobe.tsx` + `avatarCustom.ts`): 4 слота внешности
  (причёска/борода/верх/аксессуар), цены 2000/1000/1000 ₽, медаль — за 3 ачивки.
  Экшен `customize_avatar`, состояние — `player.avatar`.
- **Сезонный дроп**: гирлянда + ёлка, гейт `isFestiveSeason()` — открываются каждый декабрь.

### Офис

Экран «Мой офис»: 11 слотов (фон по типу компании, город за окном, канбан-доска,
команда, стол, коллеги из NPC, кофе, настроение дня) + контекстные действия.
Рендер — `OfficeRenderer.tsx`, ассеты — `tools/generate_office_assets.py`.

### Пиксельный аватар (альтернативный пайплайн)

Вместо рукотворных слоёв — **пиксельные JSON-описания**. Полное ТЗ: [docs/pixel-art.md](docs/pixel-art.md).

Цепочка: `prompt → AI → components.json → валидатор → generator_config.json → рендер → PNG + manifest.json → игра`.

- Канвас **32×32, анфас**; слои `face → eyes → mouth → clothing → hair → hat → accessory`,
  каждый следующий полностью перекрывает предыдущий; прозрачность = отсутствие пикселя.
- **30 MVP-компонентов** (3/5/4/6/5/4/3) + 4 цветовые схемы → 86 400 комбинаций.
- Детерминизм: персонаж = `seed` + `sha256(components.json)` + `sha256(generator_config.json)`;
  `pixelgen repro` перерисовывает партию и сверяет пиксели побайтово.
- Совместимость — только `excludes` (капюшон ⇒ `hair_bald`); взаимные исключения запрещены.
- Один рендерер на CLI и браузер (`shared/engine/pixelArt.ts`), апскейл — только nearest neighbour.
- Контент отдаёт `GET /api/content/pixel`; AI-вывод идёт через `pixelgen import`
  (пиксель вне канваса и «цвет словом» — ошибка, а не warning).

### Шар-карточка

Canvas-рендер комнаты + аватара + статистики → PNG → шеринг через Telegram WebApp API
(в браузере — скачивание). 4 темы рамок: minimal, neon, gold (ачивка «Первый миллион»),
meme (ачивка «50 событий»); выбор хранится в localStorage.

---

## 🔗 Solana и NFT (mock)

- Покупка NFT-предмета (Herman Miller, MacBook…) → минт Metaplex-style metadata
  в локальный реестр `packages/server/data/nft_registry.json`; инвентарь виден в кошельке.
- **Cross-collection синергии** (`cross_collections.json`): SMB Gen2 → +5% к фрилансу,
  Mad Lads → постер в комнату, DeGods → пёс. В dev-режиме коллекции переключаются моком.
- Замена `MockNftProvider` на Helius RPC — без переделки роутов (интерфейс `NftProvider` готов).
- Кошельки: привязка адреса (`/nft/bind-wallet`), `local:<telegramId>` в dev-режиме.

---

## 📱 Клиент

React 18 + TypeScript + Tailwind + Zustand. Состояние — `store/gameStore.ts`
(все экшены с хаптикой: тап/покупка/оффер/ошибка). Дизайн-документ: [docs/design.md](docs/design.md)
(роадмап §15 — 10/10 закрыт).

### Экраны

| Экран | Файл | Что внутри |
|---|---|---|
| Меню | `MainMenu.tsx` | hero = собственная комната + аватар (рендер движком), прогресс, «Играть» |
| День | `DayView.tsx` | событие с выбором, действия по категориям, кормление питомца, банк дней, MainButton «Завершить день» |
| Навыки | `SkillsView.tsx` | дерево 34 навыков (аккордеоны), выбор основного, перки, английский |
| Карьера | `CareerView.tsx` | лестница грейдов из контента, офферы, отклики, квиз, компании, `CareerPressureCard` |
| Офис | `OfficeView.tsx` | слойный рендер офиса, команда, задачи, настроение |
| Дом | `RoomView.tsx` | комната + редактор + гардероб + питомец + NFT + шар-карточка |
| Магазин | `ShopView.tsx` | товары с превью слоёв и бейджами 🎨/🧍/🔗, жильё с превью фона |
| Трофеи | `AchievementsView.tsx` | 17 ачивок с прогрессом |
| Топ | `LeaderboardView.tsx` | живой рейтинг игроков |

### Telegram-интеграция (`lib/telegram.ts`)

- `initTelegramApp()`: `ready()` + `expand()`, тёмный chrome, `disableVerticalSwipes()`,
  синхронизация высоты вьюпорта.
- **MainButton**: нативная кнопка «Завершить день N →» с прогрессом; в браузере — sticky-CTA.
- **BackButton**: таб → «День» → меню → скрыта.
- **Хаптика** через `haptic()`: табы/выбор — `selection`, действия — `tap`,
  покупки/конец дня — `medium`, оффер/ачивка — `success`, ошибки — `error`.
- Тач: кнопки ≥ 44pt, `active:scale`, safe-area, `touch-action: manipulation`, 480px-рамка на десктопе.

### Состояния и обратная связь (`components/ui.tsx`)

Общие `Spinner`, `Skeleton`, `RoomSkeleton` (скелетон-квадрат комнаты),
`EmptyState` (с `bare`-режимом для использования внутри карточек). Пустышки с подсказками:
нет офферов → «откликнись на вакансии», нет NFT → «загляни в магазин», сытая/голодная морда питомца.

---

## 🖥️ Сервер и API

Fastify 4, сервер-авторитет. Контент валидируется Zod-схемами **при старте**
(15 групп файлов + cross-reference: эффекты `skill.X`/`relation.X`, id веток, id слоёв,
id навыков в вопросах, монотонность карьерной лестницы, каталог Stars). Состояние —
`packages/server/data/<telegramId>.json` (атомарная запись tmp+rename + in-memory кэш;
интерфейс под замену на PostgreSQL), при загрузке прогоняется через миграции.

### Auth

- `POST /api/auth/telegram` — обмен initData на JWT (HMAC-валидация при заданном `BOT_TOKEN`).
- Dev-режим: пустой токен → initData парсится без проверки подписи (id сохраняется,
  а не схлопывается в Dev/1, как раньше).
- Rate limit: 1000 req/мин на IP (`@fastify/rate-limit`), настраивается через env.
- Идемпотентность действий: `idempotencyKey` хранится **в самом сейве** (64 ключа, TTL 6 ч),
  поэтому повтор запроса безопасен и после рестарта сервера.

### Game (`/api/game`)

| Метод | Путь | Назначение |
|---|---|---|
| GET | `/state` | состояние игрока (+ `careerOutlook`, `costOfDay`, `offlineDaysEarned`) |
| POST | `/action` | универсальный экшен `{actionId, params, idempotencyKey}` |
| POST | `/advance-day` | завершить день: зарплата, расходы, офферы, событие |
| POST | `/event-choice` | выбор в событии |
| POST | `/unlock-perk` | открыть перк |
| POST | `/main-skill` | выбрать основной навык |
| POST | `/interview/start|answer|finish` | квиз собеседования |
| POST | `/reset` | сбросить прогресс |

Экшены `/action` (28): `work_task`, `work_overtime`, `freelance`, `side_job`,
`study_book|course|mentor|stepik|advanced_course`, `study_english|english_course`,
`rest_sleep|walk|gym|bar|hobby`, `networking`, `pet_project`, `apply_job`,
`cancel_application`, `accept_offer`, `decline_offer`, `buy_item`, `upgrade_housing`,
`feed_pet`, `customize_room`, `customize_avatar`, `use_banked_day`, `cto_elect`.

### Content (`/api/content`)

`manifest`, `companies`, `items`, `genetics`, `layers` (avatar+room+office),
`npcs`, `cross-collections`, `skills`, `career-gates`, `pixel`, `side-jobs`, `perks`, `achievements`.

### Leaderboard (`/api/leaderboard`)

`GET /friends|honest?limit=&offset=` — рейтинг из персистентных состояний через
**кэшируемый индекс** (полный скан не чаще раза в `LEADERBOARD_TTL_MS`, запись в индекс
на каждом сохранении). Ранжирование идёт по авторизованному пользователю: `isYou` больше
не врёт, а своя строка приходит отдельным полем `you`, даже если ты вне топ-20.
`honest` — доска без владельцев booster-предметов.

### Payments (`/api/payments`) — Telegram Stars

| Метод | Путь | Назначение |
|---|---|---|
| GET | `/products` | каталог из `content/monetization.json` + что уже куплено |
| POST | `/stars` | `createInvoiceLink` (XTR) на выбранный товар |
| POST | `/webhook` | обновления Telegram: `pre_checkout_query` и `successful_payment` |
| POST | `/dev-complete` | локальная симуляция покупки (только `ALLOW_MOCK_PAYMENTS`) |

Вебхук аутентифицируется секретом из `setWebhook` (`X-Telegram-Bot-Api-Secret-Token`,
сравнение constant-time), pre-checkout подтверждается (иначе Telegram отменяет платёж),
начисление идемпотентно по `telegram_payment_charge_id` (реестр `data/entitlements.json`).
Схема каталога запрещает продавать прогресс: только косметика, дни в банке и значки.

### Admin (`/api/admin`) — для бота

`POST /nudges` (кого позвать обратно + отметка «уведомлён»), `GET /users/:id/summary`
(данные для `/stats`), `DELETE /users/:id` (GDPR). Доступ по `x-admin-token`;
без `ADMIN_TOKEN` весь namespace отвечает 503, а не работает без защиты.

### Прочее

- `/api/nft/*` — `bind-wallet`, `inventory`, `cross-collections`, `mock-collections`.
- `GET /health` — `{status, version, mode}` (`mode: 'dev'` = initData не проверяется).

---

## 🤖 Telegram-бот

`packages/bot` — настоящий бот на Bot API (`fetch`, без зависимостей). Режим выбирается сам:

| Режим | Когда | Что делает |
|---|---|---|
| `polling` | задан `BOT_TOKEN` (по умолчанию) | long polling `getUpdates`, регистрирует команды через `setMyCommands` |
| `webhook` | `BOT_MODE=webhook` + `BOT_WEBHOOK_URL` | сам вызывает `setWebhook`, проверяет секретный заголовок |
| `mock` | нет `BOT_TOKEN` | `POST /webhook` отвечает тем, что **было бы** отправлено — вся логика тестируется офлайн |

Команды: `/start` (приветствие + кнопка `web_app`, deep link `startapp=ref_<id>`),
`/play`, `/help`, `/stats` (живая сводка через `/api/admin/users/:id/summary`),
`/delete_my_data` (реальное удаление сейва). Плюс фоновая рассылка «банк дней переполнен»:
бот раз в час забирает кандидатов из `POST /api/admin/nudges` — сервер сам помечает их
уведомлёнными, поэтому дважды одному и тому же человеку не прилетит.

Обработчики команд — чистые функции (`src/commands.ts`), покрыты 22 тестами.

---

## 📝 Контент

Всё в `packages/content/` — JSON без кода. Изменения подхватываются рестартом сервера.

| Файл | Содержимое | Объём |
|---|---|---|
| `events/` | игровые события | **205 шт в 19 файлах** |
| `balance.json` | баланс v2.1: гейты, расходы, налог, финалы, жильё | — |
| `skills.json` | навыки | 34 в 9 ветках |
| `items.json` | предметы, курсы, питомцы, аксессуары | 30 |
| `companies.json` | компании | 10 |
| `npcs.json` | NPC | 5 |
| `achievements.json` | ачивки | 17 |
| `perks.json` | перки | 8 |
| `challenges.json` | daily-челленджи | 13 |
| `interview_questions.json` | вопросы квиза | 56 |
| `cross_collections.json` | NFT-синергии | 3 коллекции |
| `genetics.json` | генетика внешности | — |
| `layers/*.json` | манифесты слоёв | avatar 36 / room 50 / office 53 |
| `pixel/` | пиксельные компоненты + конфиг генератора | 30 компонентов |
| `monetization.json` | каталог Telegram Stars (косметика, дни в банке, значки) | 4 товара |

Валидация контента — часть самого пакета:

```bash
npm run validate:content              # схемы + cross-reference (ошибки → exit 1)
npx tsx packages/content/src/cli.ts --strict   # предупреждения тоже фатальны (CI на PR)
npx tsx packages/content/src/cli.ts --json     # машиночитаемый отчёт
```

---

## 📊 Баланс и симулятор

Баланс получен прогоном симулятора (40 агентов × 365 игровых дней, «разумный игрок»).

| Веха | Медиана | Цель |
|---|---|---|
| Первый оффер | 18 | 16 |
| Junior | 33 | 30 |
| Middle | 74 | 74 |
| Senior | 142 | 150 |
| Teamlead | 210 | 205 |
| Архитектор | 299 | 285 |
| Жильё 1 / Жильё 2 | 162 / 256 | 203 / 286 |

Комплаенс поздней игры: architect до 250-го дня ≤ 25%, доля архитекторов ≥ 50%,
CTO за год ≤ 35% (эндгейм обязан оставаться трудным), выгорания ≤ 20%, тупиков 0.

```bash
npm run simulate          # отчёт симулятора
npm test                  # контент + 201 юнит-тест (движок/сервер/бот) + simulate --check
npx tsx packages/sim/src/simulate.ts --why   # что блокирует агента на следующем гейте
```

Что изменилось в v2.1 по сравнению с v2.0 (было: 100% игроков — архитекторы, деньги ничего не значили):

- **Грейды — данные, а не код** (`careerGates`): глубина основного навыка, ветка, ширина,
  мягкие навыки, репутация, период ревью, конкуренция.
- **Повышение ≠ автовыдача**: требования открывают ревью, решает слот с вероятностью от перебора.
- **Стоимость жизни работает**: ежедневные расходы, режим «гречка», налог на капитал, переезды-привычки.
- **Мягкие навыки не фармятся**: кап нетворкинга + демпфер насыщения.
- **Финалы достижимы**: выборы CTO, терминальные `burnout`/`left_it`.

---

## 🛠️ Инструменты

```bash
# Пиксельные персонажи
npm run pixelgen:audit      # compile-идемпотентность + схема + структура + 64 комбинации (CI-гейт)
npm run pixelgen:demo       # 48 персонажей → artifacts/pixel (PNG, spritesheet, gallery.html)
npm run pixelgen:render -- face=face_angular hair=hair_manbun hat=hat_beanie   # ASCII-превью
npm run pixelgen:prompt -- hair --count=2   # мастер-промпт для генерации новых компонентов

# SVG-слои (Python, детерминированные; художник правит результат, код не трогаем)
python3 tools/generate_layer_assets.py    # комната + аватар → packages/client/public/layers/
python3 tools/generate_office_assets.py   # офис → packages/client/public/layers/office/

# Статистика событий
npx tsx packages/sim/src/events_stats.ts  # покрытие событий за прохождение
```

---

## ✅ Проверки

```bash
npm run lint            # типы (tsc по всем пакетам) + eslint
npm run lint:types      # только tsc
npm run lint:code       # только eslint
npm run format          # prettier --write .
npm run validate:content  # схемы контента + cross-reference
npm test                # контент + vitest (shared/server/bot) + симулятор баланса
npm run build           # полная сборка (shared → content → server → client → bot → sim)
npm run pixelgen:audit  # пиксельный контент: идемпотентность + валидация
```

Тесты: **201** (`shared` 128 · `server` 51 · `bot` 22) + симулятор `--check`.

CI — [`ci/github-actions-ci.yml`](ci/github-actions-ci.yml) (скопируйте в `.github/workflows/ci.yml`,
см. [ci/README.md](ci/README.md)): на каждый push и PR гоняются
типы, eslint, prettier (`continue-on-error`), валидация контента, все три набора тестов,
симулятор, `pixelgen:audit` и сборка. Отдельная джоба `content-guard` включается, только
если PR трогает `packages/content/**`: там валидация в `--strict` (предупреждения фатальны)
и обязательный прогон симулятора баланса.

---

## 🗺️ Карта документации

| Документ | О чём |
|---|---|
| [SPEC.md](SPEC.md) | ТЗ v2.0: стек, сервер-авторитет, офлайн-энергия, контент как данные, карьерные гейты, пиксельная генерация |
| [DESIGN.md](DESIGN.md) | процедурная генерация (комбинаторика + тинтинг + on-chain seed) и Solana-интеграция |
| [docs/design.md](docs/design.md) | дизайн-документ клиента: 8 экранов, комната/офис/гардероб/питомец, полировка под TMA, роадмап §15 |
| [docs/pixel-art.md](docs/pixel-art.md) | ТЗ пиксельного пайплайна: канвас, слои, палитры, excludes, воспроизводимость |
| [docs/deploy.md](docs/deploy.md) | деплой: переменные окружения, systemd/nginx, вебхуки Stars, бэкапы, GDPR, чек-лист релиза |
| [ANALYSIS-2026-09-07.md](ANALYSIS-2026-09-07.md) | аудит проекта на живой сборке: архитектура, баги, риски, late-game баланс |

---

## 📋 To-Do: что сделано, что в процессе, что предстоит

### ✅ Реализовано

**Ядро и сервер**
- [x] Сервер-авторитет (Fastify): `/state`, `/action` (29 экшенов), `/advance-day`,
  `/event-choice`, перки, основной навык, квиз, `/reset`
- [x] Персистентность в JSON (`server/data/`, атомарная запись + кэш) — переживает рестарты
- [x] Auth: Telegram initData (HMAC) → JWT + dev-фолбэк; rate limit; идемпотентные действия
  (ключи живут в сейве, а не в памяти процесса)
- [x] Контент как данные: Zod-валидация 15 групп + cross-reference проверки; общий загрузчик
  `@itsim/content` для сервера и CI, CLI `validate:content` (`--strict`, `--json`)
- [x] Миграции сейвов (`migrateState`, версия 2): нормализация, идемпотентность, защита от
  сейвов «из будущего»
- [x] Безопасная конфигурация: `config.ts` + отказ стартовать в проде с dev-дефолтами
- [x] Лидерборд на кэшируемом индексе: пагинация, честная доска, свой ранг, `isYou` не врёт

**Игровые системы**
- [x] 5 ресурсов, энергия-ход + офлайн-банк дней (3.5 ч/день, кап 7)
- [x] Карьерные гейты из контента + ревью + конкуренция за слот + выборы CTO + терминальные финалы
- [x] Стоимость жизни: ежедневные расходы, «гречка», налог на капитал, переезды-подушки, доходный барьер
- [x] 205 событий в 19 файлах: цепочки, кулдауны, теги, NPC-условия, follow-up'ы на действия
- [x] 34 навыка / 9 веток, основной навык, 8 перков, мягкие навыки с насыщением, английский
- [x] Квиз собеседований: 56 вопросов, topic-first, XP за ответы, честное влияние на оффер
- [x] 10 компаний, офферы со сгоранием, отклики, увольнения, 5 NPC с отношениями
- [x] Фриланс, 7 подработок, майнинг-ферма (оборудование, электричество, панели, волатильность)
- [x] 17 ачивок, 13 daily-челленджей, живой лидерборд
- [x] Питомцы 2.0: 6 видов, кормление, настроение, анимация, 3 аксессуара, статус-чип

**Клиент и визуал (роадмап дизайна §15 — 10/10)**
- [x] 7-й таб «Топ», hero меню = своя комната + аватар, магазин с превью слоёв и бейджами
- [x] Редактор комнаты, гардероб (4 слота, цены, медаль за ачивки), офис на 11 слотов
- [x] Слойные манифесты avatar/room/office, генетика по seed'у, тинтинг
- [x] Пиксельный пайплайн: 30 компонентов, валидатор, генератор, `repro`, браузерный рендер
- [x] Сезонный дроп (гирлянда + ёлка, декабрьский гейт), шар-карточка с 4 темами рамок
- [x] TMA-полировка: MainButton, BackButton, хаптика, safe-area, спиннеры/скелетоны/пустышки

**Монетизация и бот**
- [x] Telegram Stars: каталог в контенте (только косметика/дни/значки — схема запрещает
  pay-to-win), `createInvoiceLink`, аутентификация вебхука, `pre_checkout_query`,
  идемпотентные начисления, реестр покупок, секция в магазине
- [x] Настоящий бот: Bot API (polling/webhook/mock), `/start` с deep link, `/play`, `/help`,
  `/stats`, `/delete_my_data`, уведомления «банк дней переполнен»
- [x] Служебное API `/api/admin/*` под `ADMIN_TOKEN` (сводка, nudges, GDPR-удаление)

**NFT/Solana (mock)**
- [x] Минт предметов в локальный реестр, инвентарь в кошельке, 3 cross-collection синергии,
  интерфейс `NftProvider` под Helius

**Качество**
- [x] 201 юнит-тест (движок 128 · сервер 51 · бот 22), симулятор 40×365 с `--check`-гейтом,
  `simulate --why`
- [x] ESLint 9 + Prettier; `lint` = типы + линтер (раньше был только `tsc`)
- [x] CI GitHub Actions: типы, линтер, контент, тесты, симулятор, pixelgen-аудит, сборка
  + отдельный `content-guard` на PR, трогающих контент
- [x] `.env.example` и [docs/deploy.md](docs/deploy.md) (вебхуки, бэкапы, GDPR, чек-лист)
- [x] `lint`/`test`/`build`/`pixelgen:audit` — зелёные; API, бот и клиент проверены вживую

### 🔄 В процессе

- [ ] **PR #4 → `main`**: фаза 2 дизайна (hero меню, гардероб, питомец 2.0, сезонка,
  рамки шар-карточек, состояния) — открыт, ждёт ревью и вливания
- [ ] **Фаза 3 дизайна** (обсуждается): автопостинг вех («Стал Senior за 78 дней!»),
  скины и позиции питомца в комнате (§12.4), события зимнего дропа

### 📋 Предстоит

**Инфраструктура**
- [ ] PostgreSQL + Prisma вместо JSON-файлов (интерфейс хранилища уже узкий, миграции сейвов есть)
- [ ] Прогнать `npm run format` по всей кодовой базе (сейчас prettier в CI — `continue-on-error`,
  чтобы не смешивать переформатирование с содержательными правками)
- [ ] Реальный Solana-провайдер (Helius RPC) вместо `MockNftProvider`
- [ ] Тесты клиента (сейчас покрыты `shared`, `server`, `bot`)

**Геймплей и баланс**
- [ ] Поздняя игра: наполнить деньгами/выбором/угрозой (сейчас эндгейм трудный, но пустой);
  финалы `exit`, `free_artist`, `teacher` описаны в балансе, но не реализованы в цикле
- [ ] Детекция аномалий (античит сверх rate limit)
- [ ] Серверный RNG везде, где влияет на баланс (сейчас — только вопросы и генетика)

**Контент и визуал**
- [ ] Рукотворные арты вместо плейсхолдеров генераторов (ассет-лист — в DESIGN.md, ~170 шт)
- [ ] Анимации питомца idle/sleep/happy (сейчас — CSS-покачивание + эмодзи)
- [ ] i18n (сейчас только русский)

---

## 📜 Лицензия

MIT
