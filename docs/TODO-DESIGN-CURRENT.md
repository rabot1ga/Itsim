# Актуальный план визуального соответствия — 8 сентября 2026

Этот файл — текущий to-do. `TODO-2026-09-08-integration.md` сохраняет историю,
но его выполненные пункты не означают завершённость дизайна. Рабочая ветка:
`arena/01a08128-itsim`, интеграционный PR #7. Main напрямую не меняем.

## Два референса — две задачи

**1.png (1254×1254):** верхний рекламный баннер; затем четыре отдельных экрана
Главная, Работа, Обучение, Магазин; единая нижняя навигация с Друзьями. Ниже —
события, цели и профиль. Баннер не является заставкой игры.

**2.png (1536×1024):** десять карточек случайных событий. Формат:
категория → широкая сюжетная сцена → заголовок → короткий рассказ → цветные
решения → последствия с названиями ресурсов и знаками. Картинки не содержат
кликабельный UI: кнопки и показатели должны оставаться настоящими компонентами.

## Что подтверждено повторным обзором

| Область | Сейчас | Разрыв с референсом |
| --- | --- | --- |
| Главная | Правильная композиция, реальные показатели, новые изображения | Фронтальная комната статична; портрет использует сохранённый спрайт комнаты |
| Работа | Рабочие действия, вакансии, офферы | Нет модели проекта с дедлайном/чеклистом; нельзя подменить её фиктивным Telegram Bot 62% |
| Обучение | Компактный список/карта, XP, выбор навыка | Буквенные значки вместо полного набора пиксельных иконок |
| Магазин | Компактные строки, цена-кнопка, категории | Смешаны кропы, SVG и изометрические спрайты; одежда не отдельный товарный каталог |
| Друзья | Реальные NPC и отношения | Персонажам нужны индивидуальные портреты |
| События | 10 новых тематических сцен, выбор, подписанные эффекты и требования | Не уникальный арт для каждого события; визуальный review всех 205 историй ещё нужен |
| Профиль/цели | Профиль с реальной статистикой и переходами к достижениям | Отдельные карточки целей с наградами из контента ещё не собраны |
| Ассеты | 12 новых AI-generated изображений и 4 собственных SVG техники | Не весь каталог и не все NPC; старые вспомогательные спрайты остаются |
| Техника | Сборка, тесты, симуляция проходили в прошлой итерации | npm-уязвимости, неактивный GitHub workflow, JSON-хранилище, большой game.ts остаются |

В этой итерации повторно просмотрены оба PNG, компоненты основных экранов,
путь события клиент→сервер, схемы и фактические эффекты всего каталога событий.
Это не новый построчный security-аудит всех файлов и не ревизия удалённых веток.

## P0 — выполняется сейчас

- [x] Повторно разобрать 1.png и 2.png, отделить текущий план от истории.
- [x] Создать новый AI-generated набор из 10 сюжетных сцен без встроенного UI:
  ночь, кот, велосипед, оффер, соцсети, техника, сервер, посылка, отдых, баг.
- [x] Создать собственные фронтальную комнату и стандартный портрет героя.
- [x] Проверить изображения визуально, вырезать сцены без белых разделителей,
  сжать WebP, записать manifest с размерами, источником и контрольными суммами.
- [x] Подключить новые изображения; выбирать сцену по теме/ID/заголовку события.
- [x] Переработать EventCard: пиксельные значки, подписанные эффекты каждого
  решения, без взаимного погашения XP разных навыков и отношений разных NPC.
- [x] Заблокировать повторный выбор во время запроса; показать недоступность
  выбора по известным требованиям; сервер остаётся источником истины.
- [x] Добавить профиль с реальными показателями и переходами к целям/гардеробу.
- [x] Покрыть выбор арта/эффекты/ожидание тестами, проверить карточки в браузере.
- [x] Прогнать build, lint, unit, симуляцию и E2E; собрать обзор созданных ассетов
  и реальных экранов для сравнения с референсами.

## P1 — после этой поставки

- [ ] Фронтальная послойная комната: мебель/питомец/декор из сохранения,
  визуальные варианты по housingLevel, связь с редактором. Не заменять
  подпись «иллюстрация» заявлением о динамике до реализации.
- [x] Связать портрет с внешностью/гардеробом: в UI 08 используется тот же
  спрайт и движок перекраски, что в комнате (причёска, силуэт, цвета).
- [ ] Полное соответствие всех деталей гардероба: борода, аксессуары и точные
  варианты одежды пока ограничены существующим набором комнатных спрайтов.
- [ ] Перерисовать весь каталог товаров и навыков в едином масштабе/палитре.
- [ ] Индивидуальные портреты знакомых и профиль персонажа в событиях.
- [ ] Определить модель проектов: задачи, прогресс, срок, награда, сохранение,
  серверные проверки и баланс. Только затем экран работы как в примере.
- [ ] Карточки целей с настоящими условиями/прогрессом и наградами из контента.
- [ ] Привести все вторичные панели/модальные окна к единой системе рамок.
- [ ] Visual regression baseline, длинные тексты и крупный системный шрифт,
  safe area, клавиатура, реальный Telegram WebView.

## P2 — долги проекта, не смешивать с визуальным редизайном

- [ ] Обновить уязвимые зависимости с проверкой совместимости, без audit --force.
- [ ] Включить GitHub Actions при наличии workflow-прав.
- [ ] Разделить game.ts; переход на транзакционную БД перед multi-instance deploy.
- [ ] Проверить реальные Telegram auth/Stars/webhook; NFT остаётся mock.
- [ ] Выборочно адаптировать старый de8fa8b без отката актуального баланса.

## Критерий готовности

Не «тесты зелёные», а одновременно: совпадает композиция референса, сцена
соответствует теме, нет встроенных в картинки кнопок/цен, данные настоящие,
понятны последствия каждого решения, нет переполнения 320/390/480 px.
Новое AI-generated изображение не означает юридическую гарантию уникальности;
происхождение фиксируем явно, перед публикацией нужен обычный review ассетов.

## Результат этой итерации

- Созданы и подключены 12 новых изображений (10 сцен + комната + портрет),
  а также 4 собственных SVG-иконки техники. Общий WebP-бюджет — 225552 байта.
- EventCard использует новые сцены, пиксельные иконки, подписанные отдельные
  последствия, цвета по затратам/выгодам (не по индексу), требования и ожидание.
- Активное событие занимает область дня; комната/действия не отвлекают от выбора.
  Native MainButton завершения дня скрывается до решения; ресурс энергии остаётся
  виден в компактном HUD. Старый smoke-тест обновлён: случайное событие может
  прервать любое действие, а не только завершение дня.
- Профиль доступен нажатием на портрет и через ⋮, показывает реальную статистику
  и переходы к целям/гардеробу. Самостоятельные карточки целей с наградами ещё P1.
- Тесты добавлены для тем, требований, знаков, раздельных XP/NPC, повторного
  выбора, сетевой ошибки и профиля. Финальные результаты проверок см. ниже.

Не выполнено всё сходство целиком: динамическая фронтальная комната, генетический
портрет, проектная механика и весь унифицированный каталог остаются незакрытыми.
Новые иллюстрации не следует принимать за реализованную механику этих сцен.

### Финальные проверки UI 07

- `npm run build`, `npm run lint` — успешно.
- `npm test` — основной набор и симуляция успешны; после последнего regression-теста
  клиентский пакет повторён: 90 тестов. Суммарно 343 package-теста.
- `npm run test -w @itsim/e2e` — 18 сценариев прошли.
- `node tools/art/validate-story-assets.mjs` — 12 файлов, размеры/бюджет/SHA-256 совпадают.
- Скриншоты главной, профиля и проверочного показа реального события из контента
  просмотрены; создан отдельный обзор десяти сцен. В браузере не было pageerror.

Новые файлы: `public/art/story-v1` (изображения и manifest), `public/art/equipment`
(векторные пиксельные иконки), `docs/ART-BRIEF-2026-09-08.md` (происхождение/бриф),
`tools/art` (упаковка и проверка). Полный размер WebP-набора — около 220 KiB.

## UI 08 — сохранённый портрет

- `PlayerPortrait` подключён к HUD и профилю. Один `characterLook` и один
  `recolourSprite` для комнаты и портрета, без второго случайного генератора.
- Квадратное кадрирование головы/плеч, nearest-neighbour, стабильный размер.
  Ответ старой асинхронной отрисовки не подменяет новую внешность.
- При отсутствии генетики/manifest или ошибке изображения — явно подписанная
  стандартная иллюстрация. Смена внешности не блокирует игру.
- 5 компонентных тестов: композиция, сохранённый цвет, fallback, ошибка, гонка.
- 4 новых E2E: реальные бесплатные изменения цвета через сервер, различие
  результирующих изображений, совпадение главной/профиля, перезагрузка,
  320/390/480 px и отказ загрузки manifest.
- Существующие AI-иллюстрации не перегенерировались. Портрет использует ранее
  созданную библиотеку `/iso`; artwork provenance этой библиотеки не менялся.
- Проверены сборка клиента и общий lint (включая типы всех пакетов).
  Браузерный обзор `/home/user/itsim-ui08-portrait.png` просмотрен: pageerror нет.

Следующий визуальный приоритет: фронтальная послойная комната. Проекты/дедлайны,
полный каталог NPC и общий интеграционный/security-аудит остаются открытыми.

Итоговая повторная проверка UI 08: `npm test` — 348 тестов пакетов
(180 shared + 51 server + 22 pixelgen + 95 client), content validation и симуляция
успешны. Все 22 E2E прошли, включая сравнение изображений после двух реальных
изменений цвета. Общий lint и финальный eslint прошли, `git diff --check` чистый.
