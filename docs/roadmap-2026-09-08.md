# To-do: улучшения, переработки и монетизация Solana

> ⚠️ **Этот файл — историческая правка.** Актуальное состояние и план —
> в [TODO-2026-09-08-current.md](TODO-2026-09-08-current.md). Здесь
> сохранён оригинальный roadmap с финальными галочками и коммитами.

Порядок исполнения: **P0 — фундамент/быстрые победы**, **P1 — большие переработки**,
**P2 — монетизация-слой Solana** (после P0: нужна телеметрия и профиль-кошелёк).
Галочка `[x]` = сделано; рядом — коммит/дата.

---

## P0 — фундамент и быстрые победы

- [x] **P0.1 Онбординг новичка**: карточка «Совет дня» (дни 1–7, `screens/dayTips.ts`) + намёк
      при низкой энергии («Поспать» доступен всегда). Коммит этой сессии.
- [x] **P0.2 Ежедневный вход-стрик**: счётчик подряд идущих дней + награда за вход (растёт со
      стриком, потолок). Движок `shared/engine/checkIn.ts` (UTC+3), начисление в `/game/state`,
      баннер в «Дне» раз в день, чип-стрик в HUD. Тесты.
- [x] **P0.3 Телеметрия**: `POST /api/telemetry` (best-effort, никогда не блокирует игру) + NDJSON
      в `data/telemetry.ndjson` + `GET /api/telemetry/summary` (сводка за N часов). Клиент шлёт
      `screen_view`, `action`, `day_end`, `event_choice`, `skill_pick`. Хранилище заменяемо на Postgres.
- [x] **P0.4 Энергия-подсказки**: плашка «энергия на нуле: поспи (+X) или заверши день» —
      сделано вместе с P0.1 (лёгкая строка при energy ≤ 2).
- [x] **P0.5 E2E-смоук Playwright**: `packages/e2e` — chromium → vite :5174 → API :3005 со
      свежим `DATA_DIR` (mkdtemp, никогда не трогает dev-данные). Смоук: старт → 3 действия
      (энергия 10→5) → конец дня → «Завершить день 2» → покупка «Кактус» → проверка, что
      телеметрия реально записалась. В песочнице без доступа к cdn.playwright.dev браузер
      берётся из `@sparticuz/chromium` (npm-hosted). CI `.github/workflows/ci.yml`:
      npm ci → lint:types → lint:code → npm test → playwright install → test:e2e.
- [x] **P0.6 Доступность (срез 1)**: убрать `!min-h-[26–32px]`, перебивавшие `.touch-target`
      (44px), у навигационных чипов, строк «Вчера»/«Стоимость дня», категорий магазина, кнопок
      «Купить», кнопок карты навыков (36px) и легенды-замков; фокус-стили `:focus-visible` уже были.
      Осталось: проверить контраст 9–10px dev-текстов.
- [x] **P0.7 Карточный редизайн UI** (по референсам `docs/design-references/reference-{1,2}.png`):
      пиксельный мир (спрайты, комнаты, персонажи, ноды карты) остаётся нетронутым, а хром
      переведён с 9-slice PNG на мягкий карточный слой: токены `--radius-*`/`--shadow-*`,
      поверхности `.panel/.tile/.well/.btn/.chip/.tabbar` на CSS-градиентах, пилюли-чипы,
      «батарейка»-метры. Новые карточки случайных событий `EventCard`: тон по тегу события,
      медальон с эмодзи/пиксель-иконкой, выборы-строки с чипами эффектов (`−500 ₽`, `+2 ⚡`…).
      Документация: `docs/ui-card-system.md`, обновлены `docs/ui.md`/README/роадмап.
      Миграция классов бесшовная: все имена сохранены, экраны обновились автоматически.

## P1 — большие переработки (дизайн + код)

- [x] **P1.1 Мета-ресет «Новая жизнь»**: после финала карьеры (`POST /api/game/new-life`) — рестарт
      с дня 1, а переживают ресет: ledger `meta` (жизни, воспоминания-финалы, лучший грейд, глубочайший
      день, действия за все жизни), ачивки, Stars-покупки/бейджи, стрик входа. Мета-множитель
      `metaXpMult`: навсегда +15% XP за каждую жизнь, потолок +75% (5 жизней) — встроен в `xpGain`.
      UI: карточка финала показывает бонус и двухшаговую кнопку «Начать жизнь N» (5с отмена);
      меню показывает «жизнь N» у «Продолжить». 11 unit-тестов движка. Косметический «скин-воспоминание»
      как предмет комнаты — отложен до арт-решения (P2: cNFT/трофеи).
- [x] **P1.2 Сезоны/ивенты по времени**:
      `content/sprints.json` — 3 темы вращаются по реальным неделям (Пн–Вс, UTC+3, та же таймзона,
      что у чек-ина). Чистый движок `shared/engine/sprint.ts`: окно недели, ротация тем, прогресс по
      префиксам действий, doneJustNow, клейм раз в неделю (16 unit-тестов). Сервер: прогресс в
      `/action`, вью в `/state`, `POST /game/sprint/claim` с гвардами (активная неделя + все цели +
      один клейм; «награда сгорела» — если неделя закрылась). Клиент: карточка `SprintCard` в «Дне»
      (таймер, метры целей, кнопка награды). Живой прогон: спринт «Неделя прокачки» +12 000 ₽,
      повторный клейм отклонён. Офферы-события со сроком действия — отдельной порцией позже.
- [x] **P1.3 Архетипы-билды навыков**: «Путь фронтендера» и т.п. — готовый маршрут по галактике
      с бонусом за завершение; подсветка маршрута на карте.
- [ ] **P1.4 Коллекции-сеты в доме**: «Ретро-уголок», «Кибер-спальня»… — за набор даётся малый
      бонус; прогресс коллекций в профиле.
- [x] **P1.5 Профиль игрока**: `ProfileView` со статистикой, бейджами, переходами. Заменено
      блоком «Кошелёк» в MORE (P1.10) как приоритет.
- [x] **P1.6 Магазин — на видное место**: магазин в нижних табах (5 вкладок), больше не
      требует тапа через MORE. Бейдж в HUD — отложен до P2.1.
- [x] **P1.7 Разбить `server/routes/game.ts`**: **сделано в v1.4 (P1.14)**
      — 2475 строк → 11 файлов, max 942 строки (`actions.ts`).
- [ ] **P1.8 Постгрес/SQLite + docker + деплой** (Fly/Railway): хранилища, лидерборд, аналитика.
      Отложен до решения про хостинг.

**Дополнительно в v1.4 (после P1.7):**

- [x] **P1.9 MiningView** — экран настройки фермы (оборудование, электричество, солнечные
      панели), 7-дневный прогноз дохода через `miningDailyIncome`.
- [x] **P1.10 WalletView** — NFT-инвентарь + привязка Solana-адреса.
- [x] **P1.11 PetView** — еда, настроение, инвентарь корма, аксессуары.
- [x] **P1.12 6 финалов** — `cto`/`exit`/`free_artist`/`teacher`/`burnout`/`left_it`,
      экран `EndingView` + экшен `claim_ending`.
- [x] **P1.13 HOUSING + SHOP_CATEGORIES** — вынесены в `balance.json`, источник истины — контент.
- [x] **P1.14 game.ts split** — см. P1.7.
- [x] **P1.15 index.css cleanup** — 41 неиспользуемый селектор удалён, 2320 → 1936 строк.

## P2 — монетизация на Solana (фазы)

- [ ] **P2.1 Профиль-кошелёк / HUD-«Кошелёк»**: привязка Phantom (wallet-adapter/deeplink) →
      `walletAddress` сохраняется; показ адреса, отвязка. Dev-фолбэк. + быстрый доступ из HUD.
- [ ] **P2.2 Владение через Helius DAS**: `getHeldCollections`/`verifyOwnership` на реальный RPC
      вместо мок-реестра; бонусы коллекций из контента уже готовы.
- [ ] **P2.3 cNFT-минт косметики**: «выбить предмет в кошелёк» — сервер (кастодиан/подпись юзера) +
      Metaplex Bubblegum; стоимость минта в USDC; идемпотентность по tx-signature.
- [ ] **P2.4 Лимитированные сезонные дропы** (500 шт.) с роялти на вторичке; рандом-редкость
      только за игровую валюту, никогда за деньги.
- [ ] **P2.5 Премиум «Pro»** (Stars или USDC-подписка): банк-слоты, эксклюзив-скины, ранний доступ
      к ивентам, значок Pro.
- [ ] **P2.6 Реферальная программа**: баллы за друзей → конвертация в косметику/cNFT.
- [ ] **P2.7 Сезонные NFT-трофеи топ-N лидерборда**.
- [ ] **P2.8 Юр.рамка**: публичная политика «NFT — это косметика/собственность, не инвестиция»;
      проверка «не азарт», условия Phantom/Helius.

## Держать в уме (не входит в ближний план)

- Звуковые «клики»; режим высокой контрастности; интернационализация (EN).
- Открытие API/бот-рассылки с уведомлениями о событиях (push через Telegram bot).
- Серверный RNG везде, где влияет на баланс (сейчас — только квиз и генетика).
- PostgreSQL + Prisma (см. P1.8).

---

**Статус (v1.4):** P0 всё, P1.1–P1.3, P1.5–P1.7, P1.9–P1.15 сделаны. P1.4 и P1.8 — в
долгосрочном плане. Дальше — P2.1 (HUD-кошелёк + Phantom deeplink) и P2.3
(контент поздней игры, см. TODO-2026-09-08-current.md §4).
